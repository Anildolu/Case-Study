'use strict';
// const webfw = require("eze-e2e-webframework");
// var logger = new webfw.log('Reports Script');
// var EC = protractor.ExpectedConditions;
// var Utils = require('../utils.js')


module.exports = {
    elements: {
        signin: element(by.css('.HeaderMenu-link no-underline mr-3')),
        //carrers:element(by.id("menu-item-203")),
        // addCriteria: element(by.css('[ng-click="commissions.addCriteria()"]')),
        // commissionAddCriteriaDropdown: element.all(by.id="menu-item-203"id="menu-item-203"(".k-widget .k-input.ng-scope")).get(0),
        // PortfolioFilterBox: element.all(by.css(".k-widget.k-multiselect.multi-selector")).get(1),
        // Portfoliomultiselectbox: element(by.css('ul li.k-button > span:first-child')),
        // Custodianmultiselectbox:element.all(by.css('ul li.k-button > span:first-child')).get(1),
        // CustodianFilterBox: element.all(by.css(".k-widget.k-multiselect.multi-selector")).get(2),
        // RouteScheduleType: element(by.id('routeScheduleType')),
        // AllocationScheduleType: element(by.id('allocationScheduleType')),
        // discardButton: element(by.buttonText("Discard"))
    },
}
